# api.cili.xyz - API Worker 配置
name = "cili-api"
main = "api-worker.js"
compatibility_date = "2024-01-01"

# KV 缓存绑定
[[kv_namespaces]]
binding = "MAGNET_CACHE"
id = "你的KV_NAMESPACE_ID"  # ← 替换为你的 KV ID

# 自定义域名 (可选，也可以在 Dashboard 配置)
# routes = [
#     { pattern = "api.cili.xyz", custom_domain = true }
# ]
